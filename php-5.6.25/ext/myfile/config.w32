// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("myfile", "for myfile support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("myfile", "enable myfile support", "no");

if (PHP_MYFILE != "no") {
	EXTENSION("myfile", "myfile.c");
}

